/**
 * 菜单图标配置 — IconPark Outline 图标集
 *
 * - FALLBACK_ICON：当页面未指定 menuIcon 时使用的兜底图标
 * - 图标名直接对应 @iconify-json/icon-park-outline，在 UnoCSS 中通过 class="i-icon-park-outline-{name}" 引用
 *
 * 使用方式：在页面 definePage({ meta: { menuIcon: 'home' } }) 中声明
 */
export const FALLBACK_ICON = 'all-application'
